//  The function getPassword(), which checks password input, was introduced previously as an example of the use of static variables. Modify the
// source file Passw1.cpp, which contains the function getPassword(), by
// adding the function changePassword().This function allows the user to
// change his or her password. Save the modified source file as
// Passw2.cpp.

#include<iostream>
using namespace std;

void getPassword(){
    
}

int main(){
    
    return 0;
}