#include <iostream>  
using namespace std;  

int main(){  
    int x = 7;   
    cout<<(x<<3)<<endl;  

    return 0;  
}  

// In the above code, we have declared a variable 'x'. 
// After declaration, we applied the left shift operator to variable 'x' to shift the three-bit position to the left.

// •	Chained Assignment
// Chained assignment expression is an expression in which the same value is assigned to more than one variable by using single statement.
// For example:
// a=b=20   
//  or   
// (a=b) = 20  
