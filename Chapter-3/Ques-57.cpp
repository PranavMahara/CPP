// Create the class that imitates part of the functionality of the basic type int, call the class Int(note different capitalization).
// The only data in this class is an int variable. Include member function to initalize an Int to 0, to initalize it to an int value,
// to display it(it looks just like an int), and to add two int values